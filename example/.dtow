# This is the dtow configuration file

# You can define target directories with the equal(=) operator, like so:
root=/

# You can utilize environment variables:
home=$HOME


# You can define the default target with the dot(.) symbol.
# It is the fallback in case there are no target matches with the above:
.=$HOME
